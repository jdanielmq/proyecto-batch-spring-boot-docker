# =========================================================
# PERFIL LOCAL - Configuración para ejecución local
# =========================================================
# Activar este perfil: -Dspring.profiles.active=local

# Puerto del servidor
server.port=8585

# Configuración de MySQL LOCAL (localhost:3308)
spring.datasource.url=jdbc:mysql://localhost:3306/spring_batch_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=JdmQ1481
##Evertec.2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# CRÍTICO: Escanear entidades del JAR externo batch-entity-dto
spring.jpa.packages-to-scan=com.evertecinc.entitydto.app.batch.model.entity

# Configuración de transacciones
spring.transaction.default-timeout=30

# Logging
logging.level.root=INFO
logging.level.com.evertecinc.data.app=DEBUG

